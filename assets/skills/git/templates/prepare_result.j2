{#
# git/prepare_result.j2 - Commit Preparation Result Template
#
# Summary-only scenario (context_delivery.prepare_for_summary):
# Extract basic info from diff → reflect commit situation → write accurate message.
# - diff_stat: compact summary (file + line counts)
# - diff_content: change preview (prepare_for_summary, ~4k chars)
#}

## 📊 🔍 Raw Data (LLM Read This)

{% if submodule_info %}
{{ submodule_info }}
{% endif %}

{% if valid_scopes %}
**🎯 Valid Scopes**: `{{ valid_scopes|join('`, `') }}`
{% endif %}

{% if scope_warning %}
⚠️  {{ scope_warning }}
{% endif %}

📁 **{{ staged_file_count }} Files to Commit**

{% for f in staged_files[:20] %}
- `{{ f }}`
{% endfor %}
{% if staged_file_count > 20 %}
- ... and {{ staged_file_count - 20 }} more files
{% endif %}

{% if lefthook_summary %}
### 🪝 Lefthook Summary

```
{{ lefthook_summary }}
```

{% endif %}

### 📁 Files by Category

{% set docs = [] %}
{% set assets = [] %}
{% set packages = [] %}
{% set other = [] %}

{% for f in staged_files %}
{% if f.startswith('docs/') %}
{% set _ = docs.append(f) %}
{% elif f.startswith('assets/') %}
{% set _ = assets.append(f) %}
{% elif f.startswith('packages/') %}
{% set _ = packages.append(f) %}
{% else %}
{% set _ = other.append(f) %}
{% endif %}
{% endfor %}

| Category | Count | Examples |
|----------|-------|----------|
| 📚 docs/ | {{ docs|length }} | {{ docs[:3]|join(', ') }}{% if docs|length > 3 %}...{% endif %} |
| 🎨 assets/ | {{ assets|length }} | {{ assets[:3]|join(', ') }}{% if assets|length > 3 %}...{% endif %} |
| 📦 packages/ | {{ packages|length }} | {{ packages[:3]|join(', ') }}{% if packages|length > 3 %}...{% endif %} |
| 🔧 other | {{ other|length }} | {{ other[:3]|join(', ') }}{% if other|length > 3 %}...{% endif %} |

{% if diff_stat %}
### 📊 Diff Summary (file + line counts)

```
{{ diff_stat }}
```
{% endif %}
{% if diff_content %}
### 📝 Change Preview (summary for commit message)

```diff
{{ diff_content }}
```
{% endif %}

---

## 📋 📝 Commit Analysis Reporter (LLM Fill Placeholders Below)

### 🔎 Analysis

| Field           | Value               |
| --------------- | ------------------- |
| **Type**        | `feat` (fix/refactor/docs/style/test/chore) |
| **Scope**       | `agent` (pick from Valid Scopes above) |
| **Files**       | `{{ staged_file_count }}` files |
| **Description** | _REPLACE_WITH_SHORT_DESCRIPTION_ |

#### 📁 Files Changed ({{ staged_file_count }})

{% for f in staged_files[:20] %}
- `{{ f }}`
{% endfor %}
{% if staged_file_count > 20 %}
- ... and {{ staged_file_count - 20 }} more files
{% endif %}

#### ✍️  Message Template

**Subject:**

```
type(scope): _REPLACE_WITH_SHORT_DESCRIPTION_
```

**Body:**
- _REPLACE_WITH_CHANGE_DETAIL_1
- _REPLACE_WITH_CHANGE_DETAIL_2
- ...

---

### 🚀 Next Steps

1. 📖 Read Raw Data above and analyze the diff
2. ✍️  Fill the placeholders in Message Template
3. 💬 Present the commit message to user
4. ✅ User replies **Yes** to commit, **No** to cancel
5. 🎯 Execute:
   ```
   @omni git.smart_commit(action='approve', workflow_id='{{ wf_id }}', message='type(scope): description\n\n- detail1\n- detail2')
   ```

---

### ❓ Confirmation

**Please confirm:** Reply **Yes** to commit, **No** to cancel.
