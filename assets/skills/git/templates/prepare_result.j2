{#
# git/prepare_result.j2 - Commit Preparation Result Template (Phase 36.9)
#
#}

## Raw Data

{% if valid_scopes %}
**Valid Scopes**: {{ valid_scopes|join(', ') }}
{% endif %}

{% if scope_warning %}
{{ scope_warning }}
{% endif %}

**{{ staged_file_count }} Files to commit**

{% for f in staged_files[:15] %}
- `{{ f }}`
{% endfor %}
{% if staged_file_count > 15 %}
- ... and {{ staged_file_count - 15 }} more files
{% endif %}

{% if diff_content %}
**Diff Preview**:
{{ diff_content[:2000] }}
{% endif %}

{% if valid_scopes %}
**Scope Notice**: Use ONLY scopes from the list above.
{% endif %}

---

## Commit Analysis Reporter

### 📋 Commit Analysis

| Field           | Value               |
| --------------- | ------------------- |
| **Type**        | `feat` (or fix, refactor, docs, style, test, chore) |
| **Scope**       | `git-workflow` (pick from Valid Scopes) |
| **Description** | _REPLACE_WITH_SHORT_DESCRIPTION_ |

#### 📁 Files to commit ({{ staged_file_count }})

{% for f in staged_files[:15] %}
- `{{ f }}`
{% endfor %}
{% if staged_file_count > 15 %}
- ... and {{ staged_file_count - 15 }} more files
{% endif %}

#### 📝 Message

**Subject:**

feat(git-workflow): _REPLACE_WITH_DESCRIPTION_

**Body:**
- _REPLACE_WITH_CHANGE_DETAIL_1
- _REPLACE_WITH_CHANGE_DETAIL_2

---

**Please confirm:** Reply **Yes** to commit, **No** to cancel.
