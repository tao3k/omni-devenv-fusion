"""
{{ skill_name }}/tools.py
Auto-generated by SkillGenerator

Skill implementation with @skill_command decorators.
"""

from agent.skills.decorators import skill_command


# =============================================================================
# Core Tools
# =============================================================================


@skill_command(
    name="example",
    category="read",
    description="An example command for the {{ skill_name }} skill.",
)
async def {{ skill_name | replace('-', '_') }}_example(param: str = "default") -> str:
    """
    An example command for the {{ skill_name }} skill.

    Args:
        param: Description of the parameter

    Returns:
        Description of the return value
    """
    return f"Result: {param}"


@skill_command(
    name="help",
    category="view",
    description="Show full skill context and guidelines.",
)
async def {{ skill_name | replace('-', '_') }}_help() -> str:
    """
    Show full skill context and guidelines.
    """
    from common.skills_path import SKILLS_DIR
    from repomix import repomix

    skill_dir = SKILLS_DIR(skill="{{ skill_name }}")
    output = repomix(skill_dir, style="xml", include=["SKILL.md", "tools.py", "guide.md"])
    return output
