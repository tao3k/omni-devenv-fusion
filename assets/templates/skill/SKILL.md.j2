---
name: {{ skill_name }}
description: Use when {{ description | lower }}.
metadata:
  author: omni-dev-fusion
  version: "0.1.0"
  source: "https://github.com/tao3k/omni-dev-fusion/tree/main/assets/skills/{{ skill_name }}"
  routing_keywords:
{%- for kw in routing_keywords %}
    - "{{ kw }}"
{%- endfor %}
  intents:
    - "Use {{ skill_name }} functionality"
---

# {{ skill_name | replace('-', ' ') | title }} Skill

## Overview

{{ description }}

This skill is generated via **Omni Hybrid Generator** (Jinja2 + LLM).
It follows the Trinity Architecture v2.0 standard with `@skill_command` pattern.

## Commands

{% for command in commands | default([]) %}
### {{ command.name }}

{{ command.description | default('No description available.') }}

**Usage:**
```python
@omni("{{ skill_name }}.{{ command.name }}", {"param": "value"})
```
{% else %}
### Available Commands

Check available commands with `@omni("{{ skill_name }}.list_tools")`.
{% endfor %}

## Trinity Architecture Context

| Component | Description |
| --------- | ----------- |
| **Code** | `scripts/commands.py` - Implementation with `@skill_command` |
| **Context** | `SKILL.md` - Skill metadata in YAML Frontmatter with metadata block |
| **State** | Configured permissions: {{ permissions | join(', ') | default('none') }} |

## Usage

```python
# Run a command
@omni("{{ skill_name }}.example", {"param": "value"})

# List all commands
@omni("{{ skill_name }}.list_tools")
```

## Related Documentation

- [Skills Documentation](../../docs/skills.md) - Comprehensive guide
- [ODF-EP Protocol](../../docs/reference/odf-ep-protocol.md) - Engineering standards
