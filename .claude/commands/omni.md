---
description: Execute skill command
argument-hint: "skill.command {args}"
---

@omni(skill_command="", arguments={})

Fill in skill_command with the command (e.g., "git.status" or "terminal.run_task")
Fill in arguments with the JSON arguments if any (e.g., {"command": "ls"})
